#################
### AUTOSTART ###
#################

# DBus / environment (single source of truth — env.conf handles systemctl import)
exec dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP XDG_SESSION_TYPE

# GTK theme
exec gsettings set org.gnome.desktop.interface color-scheme prefer-dark
exec gsettings set org.gnome.desktop.interface gtk-theme Tokyonight-Dark
exec gsettings set org.gnome.desktop.interface icon-theme Papirus-Dark

exec autotiling
# Inhibit idle when game controller is connected and active
exec $HOME/.local/bin/b1air-daemon gamepad-inhibit

# Wallpaper
exec $HOME/.local/bin/b1air-daemon wallpaper restore

# Polkit agent
exec /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1

# ── Idle + Lock ───────────────────────────────────────────────────
# If swaylock-effects is available the lock script uses visual effects,
# otherwise it falls back to plain swaylock automatically.
exec bash $HOME/.config/sway/scripts/session/swayidle.sh

# UI
exec_always bash $HOME/.config/sway/scripts/core/startup.sh
